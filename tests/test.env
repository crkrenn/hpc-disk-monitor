# Test environment variables for HPC Disk Monitor tests

# Test database path (will be overridden in integration tests)
DISK_STATS_DB={{HOME}}/hpc-disk-monitor/test_data/disk_stats.db

# Test filesystem configuration
FILESYSTEM_PATHS=/tmp
FILESYSTEM_LABELS=tmpfs